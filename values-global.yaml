main:
  clusterGroupName: datacenter

global:
  pattern: industrial-edge

  options:
    useCSV: False
    syncPolicy: Automatic
    installPlanApproval: Automatic

  imageregistry:
    account: yono
    hostname: quay.io
    type: quay

  git:
    hostname: github.com
    account: yd-ono
    #username: PLAINTEXT
    email: yono@redhat.com
    dev_revision: main

  s3:
    bucket:
      name: demo
      region: ap-southeast-1
      message:
        aggregation:
          count: 50
      custom:
        endpoint:
          enabled: false
